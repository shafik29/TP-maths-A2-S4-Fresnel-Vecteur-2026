import { defineConfig } from 'vite'

export default defineConfig({
  base: '/TP-maths-A2-S4-Fresnel-Vecteur-2026/',
  build: {
    outDir: 'dist',
    assetsDir: 'assets',
    emptyOutDir: true,
  }
})
